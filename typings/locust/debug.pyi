"""This type stub file was generated by pyright."""

from typing import TYPE_CHECKING, Any

from locust import User
from locust.env import Environment

if TYPE_CHECKING: ...

class PrintListener:
    """Print every response (useful when debugging a single locust)."""

    def __init__(
        self,
        env: Environment,
        include_length: bool = ...,
        include_time: bool = ...,
        include_context: bool = ...,
        include_payload: bool = ...,
    ) -> None: ...
    def on_request(
        self,
        request_type: str,
        name: str,
        response_time: float,
        response_length: int,
        exception: Exception | None,
        context: dict[str, Any],
        start_time: float | None = ...,
        response: Any | None = ...,
        **_kwargs: Any,
    ) -> None: ...

_env: Environment | None = ...

def run_single_user(
    user_class: type[User],
    include_length: bool = ...,
    include_time: bool = ...,
    include_context: bool = ...,
    include_payload: bool = ...,
    loglevel: str | None = ...,
) -> None:
    """
    Run a single User. Useful when you want to run a debugger.

    It creates in a new locust `locust.env.Environment` and triggers any `init` or `test_start` events as normal.

    It does **not** trigger `test_stop` or `quit` when you quit the debugger.

    It prints some info about every request to stdout, and you can get additional info using the `include_*` flags

    It also initiates logging on WARNING level (not INFO, because it could interfere with the printing of requests),
    but you can change that by passing a log level (or disabling logging entirely by passing None)
    """
